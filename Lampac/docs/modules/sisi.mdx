---
title: "Модуль SISI: контент 18+ и платформы"
sidebarTitle: "SISI"
description: "SISI — модуль контента 18+ для Lampa. Плагин /sisi.js, SQLite-история и закладки, 15 платформ с маршрутами /phub, /xnx и другими."
keywords: ["SISI", "Lampa 18+", "Adult modules", "sisi.js", "NextHUB"]
---

Модуль **SISI** предоставляет контент 18+ для приложения Lampa через плагин `/sisi.js`. Ядро модуля включает SQLite-базу для истории просмотров и закладок, а также агрегирует 15 внешних платформ, каждая из которых реализована в отдельном проекте под `Modules/Adult/`. Маршруты платформ защищены лимитами WAF, а данные пользователя хранятся локально.

## Ключевые маршруты

| Маршрут | Назначение |
| --- | --- |
| `/sisi.js` | Основной плагин 18+ для Lampa 3.0 |
| `/sisi/js/{token}` | Плагин с авторизацией по токену |
| `/sisi/bookmarks`, `/sisi/bookmark/*` | Список, добавление и удаление закладок |
| `/sisi/historys`, `/sisi/history/*` | Список, добавление и удаление истории |

## Платформы и маршруты

Модуль SISI агрегирует 15 платформ. Каждая платформа регистрирует собственный корневой маршрут:

| Платформа | Маршрут | Примечание |
| --- | --- | --- |
| BongaCams | `/bgs` |  |
| Chaturbate | `/chu` | Включая `/chu/potok` |
| Ebalovo | `/elo` |  |
| Eporner | `/epr` |  |
| HQporner | `/hqr` |  |
| PornHub | `/phub`, `/phubgay`, `/phubsml` | Варианты в одном проекте |
| PornHubPremium | `/phubprem` | Тот же проект, отдельный контроллер |
| Porntrex | `/ptx` |  |
| Runetki | `/runetki` |  |
| Spankbang | `/sbg` |  |
| Tizam | `/tizam` |  |
| Xhamster | `/xmr`, `/xmrgay`, `/xmrsml` | Варианты в одном проекте |
| Xnxx | `/xnx` |  |
| Xvideos | `/xds`, `/xdsgay`, `/xdssml` | Варианты в одном проекте |
| XvideosRED | `/xdsred` | `manifest.json` содержит `"enable": false` |

Дополнительные вложенные маршруты (`/vidosik`, `/stars` и другие) см. в файлах `Controllers/*.cs` внутри каждого проекта `Modules/Adult/...`.

## Конфигурация

Секция `sisi` в `init.conf` управляет поведением модуля и плагина.

```json
{
  "sisi": {
    "lgbt": false,
    "NextHUB": true,
    "history": { "enable": false }
  }
}
```

| Параметр | Назначение |
| --- | --- |
| `lgbt` | Включить LGBT-контент в плагине |
| `NextHUB` | Показывать ссылку на модуль NextHUB в интерфейсе |
| `history.enable` | Включить локальную историю просмотров в SQLite |

## NextHUB и SISI

**SISI** (ядро + `Modules/Adult/*`) — это нативные C#-источники с фиксированными маршрутами. **NextHUB** (`Modules/NextHUB`) — отдельный модуль для сайтов на YAML с маршрутом `/nexthub`. Оба относятся к контенту 18+, но не дублируют друг друга. Подробности: [NextHUB](/modules/nexthub).

## Защита WAF

При инициализации модуля SISI автоматически добавляются лимиты WAF для путей платформ:

| Паттерн | Лимит |
| --- | --- |
| `/(sisi\|bgs\|chu\|runetki\|elo\|epr\|hqr\|phub\|ptx\|sbg\|tizam\|xmr\|xnx\|xds)` | 5 запросов в секунду |

<Note>
  Закладки и история просмотров хранятся в локальной SQLite-базе (`SisiContext`). При старте ядра SISI создаются каталоги `wwwroot/bookmarks/img` и `wwwroot/bookmarks/preview` для сохранения обложек закладок, если это включено в конфиге.
</Note>
